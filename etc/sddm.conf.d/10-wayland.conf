[General]
DisplayServer=wayland

# No CompositorCommand: SDDM discovers the Hyprland session from
# /usr/share/wayland-sessions/*.desktop (uwsm), the Fedora-compatible way. Upstream's Arch value
# (start-hyprland ... /usr/share/sddm/hyprland.lua) has no counterpart on Fedora.
